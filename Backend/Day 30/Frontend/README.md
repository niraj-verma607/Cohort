## 4 Layer od frontend

UI layer => UI dikhana aur navigation handle [pages, components]

hook layer => state and APIs manage karna [hooks]

State layer => data store [auth.context.jsx, posts.context.jsx]

API layer => Backend se communicate karne ke liye [services/auth.api.js]
