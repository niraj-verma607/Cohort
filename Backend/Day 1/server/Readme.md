1. npm init -y => For setup files for creating server
2. npm i express => install Express for creating servers
3. make js file (app.js)
4. require express (const express = require("express"))
5. const app = express() => server ban jata hai
6. app.listen(3000); => isse se server start ho jata hai aur request ke liye ready ho jata hai
